# Copy to `.env.development` for `npm run dev`, or `.env.production` for `npm run build`.
# Those files are gitignored.

# Local: leave empty so Vite proxies `/v1` to http://localhost:8080 (see vite.config.ts).
VITE_API_BASE_URL=

# Deployed API (no trailing slash). Used when building for S3:
# VITE_API_BASE_URL=http://carefl-alb16-n1msowftsytk-1345249308.ap-south-1.elb.amazonaws.com
