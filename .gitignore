# Dependencies & build
node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# Environment — never commit populated env files.
# Copy `.env.example` to `.env.development` (local) or `.env.production` (build).
.env
.env.*
!.env.example

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Cursor (may contain API keys). The example file is safe to commit.
.cursor/*
!.cursor/mcp.json.example

# Coverage / caches
coverage
.vite
.turbo
